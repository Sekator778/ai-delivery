# Secrets TEMPLATE. Copy to .secrets/keys.env and fill in the values.
# The .secrets/keys.env file itself does not go into git/the image (see .gitignore, .dockerignore).

DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=
ZAI_API_KEY=
MOONSHOT_API_KEY=
GOOGLE_VERTEX_CREDENTIALS=
LITELLM_MASTER_KEY=
